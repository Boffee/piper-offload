[project]
name = "piper-offload"
version = "0.9.0rc2"
description = "Model-agnostic GPU/CPU memory management for PyTorch — pinned-host offloading, block streaming, and LRU model caching."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.14"
keywords = ["inference", "offloading", "pytorch", "quantization", "streaming"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "torch>=2.13,<2.14",
]

[project.urls]
Homepage = "https://github.com/Boffee/piper-offload"
Repository = "https://github.com/Boffee/piper-offload.git"
Issues = "https://github.com/Boffee/piper-offload/issues"
Changelog = "https://github.com/Boffee/piper-offload/blob/main/CHANGELOG.md"

[project.optional-dependencies]
bnb = ["bitsandbytes>=0.49,<0.51"]
torchao = ["numpy>=2.3.2,<3", "torchao>=0.18,<0.19"]
gguf = ["gguf>=0.18,<0.20"]
quanto = ["optimum-quanto>=0.2.7,<0.3"]
convrot = [
    "piper-kernels[convrot]>=0.7.0rc1,<0.8",
    "torchao>=0.18,<0.19",
]
triton = [
    "triton>=3.7,<3.8; sys_platform == 'linux'",
    "triton-windows>=3.7,<3.8; sys_platform == 'win32'",
]
all = [
    "bitsandbytes>=0.49,<0.51",
    "gguf>=0.18,<0.20",
    "numpy>=2.3.2,<3",
    "optimum-quanto>=0.2.7,<0.3",
    "piper-kernels[convrot]>=0.7.0rc1,<0.8",
    "torchao>=0.18,<0.19",
    "triton>=3.7,<3.8; sys_platform == 'linux'",
    "triton-windows>=3.7,<3.8; sys_platform == 'win32'",
]

[build-system]
requires = ["uv_build>=0.11.32,<0.12"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pre-commit>=4.6.1",
    "pyright>=1.1.411",
    "pytest~=9.1",
    "ruff>=0.16.1",
    # Every adapter backend, so `uv sync` yields a complete test environment
    # (otherwise optional adapter suites silently skip).
    "piper-offload[all]",
]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.pyright]
include = ["src/piper_offload"]
exclude = ["tests"]
pythonVersion = "3.14"
typeCheckingMode = "standard"
reportMissingImports = "none"
reportPrivateImportUsage = "none"

[tool.uv.sources]
torch = [
    { index = "pytorch-cu130", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]
torchao = [
    { index = "pytorch-cu130", marker = "sys_platform == 'linux'" },
]
[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[tool.ruff.lint]
select = [
    "E", "F", "W", "I", "N", "ANN", "B", "A", "COM", "C4",
    "DTZ", "EXE", "PIE", "T20", "PT", "SIM", "ARG", "PTH", "UP",
    "ERA", "RUF", "PL",
]
ignore = [
    "ANN002",
    "ANN003",
    "ANN204",
    "COM812",
    "PTH123",
    "PLR2004",
    "N812",
]

[tool.ruff.lint.per-file-ignores]
"src/piper_offload/_triton_*_lora.py" = [
    # Triton JIT kernels expose positional pointer and constexpr parameters.
    "PLR0917",
]
"tests/**/*.py" = [
    # Tests intentionally use local helpers, optional imports guarded by
    # importorskip(), and assertion-oriented context-manager structure.
    "ANN",
    "ARG",
    "B006",
    "B007",
    "ERA001",
    "F841",
    "I001",
    "PLC0415",
    "PT001",
    "PT012",
    "PT018",
    "RUF003",
    "RUF012",
    "RUF043",
    "RUF059",
    "SIM108",
    "SIM117",
]

[tool.ruff.lint.pylint]
max-args = 10

[tool.ruff.lint.isort]
known-first-party = ["piper_offload"]
